-> You need to manually modify models/package.json

    plugins: {  // in this json key-value pair
        User : "user.js"
    }


-> You need to uncomment code for initializing passport-local
   in /configs/init/api.js

... line 18

//  var User = app.db.User; // hardcoded to use user
//  passport.use(new LocalStrategy(User.authenticate()));
//  passport.serializeUser(User.serializeUser());
//  passport.deserializeUser(User.deserializeUser());


-> You need to uncomment code for setting up routes related to auth module.

... line 2

//var passport = require('passport');

... line 8

//    app.post('/v1/login', passport.authenticate('local'), userController.login);
//    app.post('/v1/logout', userController.logout);
//    app.post('/v1/register', userController.register);
//    app.all('/v1/*', userController.checkAuth);
